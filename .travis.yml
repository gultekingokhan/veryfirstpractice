language: objective-c
osx_image: xcode9.3

before_install:
   - gem install cocoapods
   - pod install

script: 
   - xcodebuild test -workspace veryfirstpractice.xcworkspace -scheme "veryfirstpractice" -destination 'platform=iOS Simulator,name=iPhone 8,OS=11.3' CODE_SIGNING_REQUIRED=NO
